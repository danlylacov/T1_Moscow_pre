# Обнаруженные проблемы в YAML файлах

## Критические проблемы:

1. **Go файл - дублирование стадии test** (строки 67-79 и 90-105)
2. **Java migration - неправильное сообщение** (spring-boot поддерживается, но показывает "not detected")
3. **TypeScript migration - неправильное сообщение** (Express поддерживается, но показывает "not detected")
4. **pre_checks - использует Python для всех языков** (не логично)
5. **post_deploy - использует Python/pytest для всех языков** (не логично)

## Проблемы логики:

6. **Переменные - устанавливаются для всех языков** (даже если не используются)
7. **Отсутствуют зависимости между стадиями** (dependencies)
8. **Кэширование - не везде где нужно**
9. **Go - отсутствует type_check стадия** (хотя должна быть)

## Нужно исправить:
